services:
  - type: web
    name: chatbotfingusach-backend
    env: node
    rootDir: backend
    buildCommand: npm ci --omit=dev && npm run verify:pg
    startCommand: npm start
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production